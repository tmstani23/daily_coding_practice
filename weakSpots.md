# Weak Spots

Concept gaps that showed up during sessions. Review sessions (every 3rd)
prioritize these over regular reviewQueue.md items. Clear an item only
after TWO spaced clean solves (mark the first with a date; clear on the
second).

Format: `- [ ] concept — what happened (YYYY-MM-DD)`
After first clean solve, append ` | clean 1/2 (YYYY-MM-DD)` to the line.
On the second spaced clean solve, delete the line.

---
- [ ] string methods recall (split/charAt/slice) — had to look up rather than recall cold on titleCase exercise (2026-07-18) | clean 1/2 (2026-07-24)
- [ ] create-if-absent then increment — the `x || 0` default idiom for a key that may not exist yet (2026-07-22, narrowed 2026-07-25). Now tested in isolation (stone 4, 07-31): needed approach-lookup help to reach `(counts[key] || 0) + 1`. Tim confirmed (07-31) the actual blocker was two things: (a) not knowing `||` itself — how it evaluates/short-circuits on a falsy left side, and (b) not knowing the expression needed explicit grouping parens around `counts[key] || 0` before `+ 1` — without them precedence doesn't do what you'd want. Narrower than "the idiom" broadly; these are the two sub-facts to check next time. STILL NOT SOLIDIFIED (2026-08-05): could trace it correctly when walked through a concrete example on stone B, but Tim flagged he's only seen it once or twice and wants dedicated spaced reps — do not treat a successful trace-through as equivalent to cold recall; needs its own review-queue slots to actually solidify, not just incidental exposure inside a bigger exercise.
- [ ] reduce's non-match branch must return the accumulator UNCHANGED, not reset/omit it — surfaced at the #20 primer-check (verbal mini-scenario): said the else branch of a conditional number-accumulator reduce returns `0`, instead of passing the accumulator through. Distinct from the block-body-shape gap below (that one is "remember to `return` at all"; this one is "don't clobber what you're returning"). Cleared same-session on `stone-reduce-else-passthrough` (sumPositives, 2026-08-11) with no help | clean 1/2 (2026-08-11)
- [ ] reduce's BLOCK-BODY callback shape — an arrow function with `{ }` needs an explicit `return` (a single-expression arrow with no braces doesn't); reduce's initial-value argument goes OUTSIDE the callback as reduce's own second argument, not inside the callback body. First time reduce needed a block body — the only prior use (stone A, 08-04) was a single-expression callback with implicit return, so this move was never exercised (2026-08-05). Drilled 08-09 (stone-reduce-block-body, doublePositives), still open — NOT self-corrected: the "mutate conditionally, then return the accumulator unconditionally and separately" structure was handed to him directly (approach lookup). Tim confirmed post-session he'd likely have assumed the accumulator updates implicitly (no return needed) or reached for `concat` instead — the need for an explicit, separate, unconditional `return acc` at the end of a mutate-in-place reduce is still not a given.
- [ ] over-applying the `x || 0` default-idiom pattern to shapes that don't call for it — defaulted to `acc = cond || acc.push(...)` (assignment + `||` as pseudo control-flow) to conditionally mutate an array accumulator, instead of a plain `if` + mutate + unconditional `return`. Tim's own diagnosis: the idiom's been drilled hard recently (stone 4, countByStatus, tallyStatuses) and he pattern-matched to "another `||` problem" rather than reading what this shape needed. Also didn't know `push()` returns the new length (not the array), which is what broke the reassignment attempt (2026-08-09)
- [ ] dot vs. bracket notation for a variable-keyed READ OR WRITE, RECURRING — same slip as 07-29/07-31 (`counts.key` instead of `counts[key]`) resurfaced 08-02 on the WRITE side too (`finalObj.key += 1` instead of `finalObj[key] += 1`); confirmed not scoped to reads only, not yet solid under added load
- [ ] computed property names (`{ [key]: value }`) confused with template literal syntax (`` `${key}` ``) — both use a similar "value inside a delimiter" shape but are unrelated features; one builds an object key, the other interpolates into a string (2026-07-31)
- [ ] CAPTURING a method's return value at all, RECURRING and now the top gap — not "which method returns what" but forgetting that a method call produces a value you must assign, return, or chain. Three instances in one drill on 08-03: a block-body arrow `w => { w[0].toUpperCase() }` with no `return`; a bare `finalSentence[0][0].toUpperCase();` on its own line; a bare `finalSentence.join(' ');` whose result was dropped while the array was returned instead. Related root cause surfaced the same day: Tim did not know STRINGS ARE IMMUTABLE, so he expected these calls to modify in place. Sibling of the two entries below, but broader — those are about picking the right method, this is about using the result of whichever method was picked (2026-08-03)
- [ ] array method RETURN VALUES — reached for forEach when a returned array was needed (forEach returns undefined, map returns the array); also passed the wrong separator to join. Picking the method by what it RETURNS, not just what it iterates (2026-07-24)
- [ ] choosing the iteration method by what it RETURNS, not "it loops" — used `map` twice on 07-30 where its return value was discarded: once as a forEach-style loop mutating an external accumulator (filter+reduce was the shape), once planned over every word when only the FIRST element needed transforming (map applies the same transform to all). Sibling of the return-values gap below; the new part is picking between map / filter / reduce / forEach / direct index access by shape of the job (2026-07-30)
- [ ] method selection by RECEIVER TYPE (array vs string) — reached for `join` to append '...' to a string; join is an array method, combining two strings is a different operation. Sibling of the return-values gap above: both are "pick the method by what it operates on / gives back", not by what it vaguely does (2026-07-27)
- [ ] reduce mechanics — didn't know `reduce` throws on an empty array without an explicit initial-value second argument, and initially typed the accumulator's element as the raw filtered object instead of the field on it (`currentValue` vs `currentValue.hoursWorked`) — first real use of reduce as a chosen (not spec-dictated) method (2026-08-04)
- [ ] combining spread-accumulate with a computed key in ONE object literal (`{...accum, [key]: value}`) — spread-override and computed-key writes are each separately solid, but stacking both together in a reduce accumulator broke on first contact: wrote `{accum[currentId]: value}` (used a property access as a key — invalid syntax), then `{...accum[currentId] = value}` (spread wrapped around an assignment, would drop prior entries). Needed a plain-English walkthrough ("two things side by side in one literal") to land on the working form (2026-08-15, indexById)
- [ ] read-modify-write: forgetting the READ half entirely when overwriting a key — wrote `{...stats, [statName]: + 1}` on the review revisit of increment-a-known-key (2026-08-16, bumpStat); bracket notation on the write was correct (the 07-29 slip didn't recur), but the right side of the colon never referenced `stats[statName]` at all, just the literal `1`. Different failure mode than the original dot-vs-bracket gap on the same topic — this is skipping the "read the current value" step, not a notation slip.
- [ ] dot vs. bracket notation for a variable-keyed READ inside a combined expression — wrote `counts.key` (accesses a property literally named "key") instead of `counts[key]`, causing NaN; first time reading and writing the same variable key together in one expression, under the added load of the arithmetic (2026-07-29)
